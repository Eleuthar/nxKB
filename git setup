$ ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
# add SSH key in account setting
$ eval "$(ssh-agent -s)"
$ ssh-add ~/.ssh/<qname>
$ ssh -T git@github.com


REPO MANAGEMENT
`````````````````
$ git branch
    \\ current branch marked by asterisk

$ git checkout main
    \\ switch to master before working
    
$ git pull
    \\ refresh main branch

$ git checkout <working-branch>
    \\ switch back to working branch
    
$ git pull
    \\ update working branch

$ git rebase main
    \\ update working branch from main

$ git checkout -b <new-branch>
    \\ make & switch to new working branch
